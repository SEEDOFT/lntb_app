import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF16A34A); // Green from asset pack
  static const Color secondary = Color(0xFF1E3ABA); // Blue from asset pack
  static const Color accent1 = Color(0xFF84CC16);
  static const Color accent2 = Color(0xFF0EA5E9);

  static const Color background = Colors.white;
  static const Color textPrimary = Color(
    0xFF334155,
  ); // Dark gray from asset pack
  static const Color textSecondary = Color(0xFF64748B);
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
  static const Color inputBorder = Color(0xFFE5E7EB);
  static const Color inputFill = Color(
    0xFFF1F5F9,
  ); // Light gray from asset pack
}
